@model Inventarios.Models.Herramienta
@{
    ViewData["Title"] = "Crear Nueva Herramienta";
}

<div class="row justify-content-center">
    <div class="col-lg-8">
        <!-- HEADER -->
        <div class="mb-4">
            <h1>➕ Crear Nueva Herramienta</h1>
            <p class="text-muted">Registra una nueva herramienta en el inventario</p>
        </div>

        <!-- FORMULARIO -->
        <div class="form-container">
            <form asp-action="Crear" method="post" novalidate>
                <!-- Validación General -->
                <div asp-validation-summary="ModelOnly" class="alert alert-danger alert-dismissible fade show" role="alert">
                    <strong>⚠️ Error en el formulario:</strong>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>

                <!-- SECCIÓN: INFORMACIÓN BÁSICA -->
                <fieldset class="mb-4 p-3 border rounded bg-light">
                    <legend class="mb-3">📝 Información Básica</legend>

                    <!-- Código -->
                    <div class="form-group mb-3">
                        <label asp-for="Codigo" class="form-label">📦 Código</label>
                        <input asp-for="Codigo" class="form-control" 
                               placeholder="Ej: HERR-001" required />
                        <small class="form-text text-muted">Código único para identificar la herramienta</small>
                        <span asp-validation-for="Codigo" class="text-danger"></span>
                    </div>

                    <!-- Nombre -->
                    <div class="form-group mb-3">
                        <label asp-for="Nombre" class="form-label">🏷️ Nombre</label>
                        <input asp-for="Nombre" class="form-control" 
                               placeholder="Ej: Destornillador Phillips" required />
                        <small class="form-text text-muted">Nombre descriptivo de la herramienta</small>
                        <span asp-validation-for="Nombre" class="text-danger"></span>
                    </div>

                    <!-- Marca -->
                    <div class="form-group mb-3">
                        <label asp-for="Marca" class="form-label">🎯 Marca</label>
                        <input asp-for="Marca" class="form-control" 
                               placeholder="Ej: Stanley, Bosch, etc." />
                        <small class="form-text text-muted">Marca del fabricante (opcional)</small>
                        <span asp-validation-for="Marca" class="text-danger"></span>
                    </div>
                </fieldset>

                <!-- SECCIÓN: UBICACIÓN Y CANTIDAD -->
                <fieldset class="mb-4 p-3 border rounded bg-light">
                    <legend class="mb-3">📍 Ubicación y Cantidad</legend>

                    <!-- Estante -->
                    <div class="form-group mb-3">
                        <label asp-for="Estante" class="form-label">📍 Estante</label>
                        <select asp-for="Estante" class="form-select" required>
                            <option value="">-- Selecciona un estante --</option>
                            <option>Estante A</option>
                            <option>Estante B</option>
                            <option>Estante C</option>
                            <option>Estante D</option>
                        </select>
                        <small class="form-text text-muted">Ubicación de almacenamiento</small>
                        <span asp-validation-for="Estante" class="text-danger"></span>
                    </div>

                    <!-- Cantidad Total -->
                    <div class="form-group mb-3">
                        <label asp-for="CantidadTotal" class="form-label">📊 Cantidad Total</label>
                        <input asp-for="CantidadTotal" class="form-control" 
                               type="number" min="1" value="1" required />
                        <small class="form-text text-muted">Cantidad de unidades disponibles</small>
                        <span asp-validation-for="CantidadTotal" class="text-danger"></span>
                    </div>
                </fieldset>

                <!-- SECCIÓN: ESPECIFICACIONES Y OBSERVACIONES -->
                <fieldset class="mb-4 p-3 border rounded bg-light">
                    <legend class="mb-3">📋 Especificaciones (Opcional)</legend>

                    <!-- Especificación -->
                    <div class="form-group mb-3">
                        <label asp-for="Especificacion" class="form-label">⚙️ Especificación</label>
                        <textarea asp-for="Especificacion" class="form-control" 
                                  placeholder="Ej: Tamaño, voltaje, capacidad, etc."
                                  rows="2"></textarea>
                        <span asp-validation-for="Especificacion" class="text-danger"></span>
                    </div>

                    <!-- Observaciones -->
                    <div class="form-group mb-3">
                        <label asp-for="Observaciones" class="form-label">📝 Observaciones</label>
                        <textarea asp-for="Observaciones" class="form-control" 
                                  placeholder="Ej: Estado, limitaciones, notas importantes"
                                  rows="2"></textarea>
                        <span asp-validation-for="Observaciones" class="text-danger"></span>
                    </div>
                </fieldset>

                <!-- BOTONES DE ACCIÓN -->
                <div class="d-flex gap-2 justify-content-between">
                    <a asp-action="Index" class="btn btn-secondary btn-lg">
                        ← Volver al Listado
                    </a>
                    <button type="submit" class="btn btn-success btn-lg">
                        💾 Crear Herramienta
                    </button>
                </div>
            </form>


        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
